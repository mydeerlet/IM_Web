<?xml version="1.0" encoding="UTF-8"?>
	<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
		"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
		
	<!-- namespace表示命名空间 ，值必须为接口的名称（包括包名）-->
	<!-- 实现类没有，mybatis框架会生成接口的实现类 -->
	<mapper namespace="dao.UserDao">

		
		<!--User findUser(User  user); -->
		<!-- 方法名 -->					<!-- 返回类型 -->				<!-- 参数类型 -->
		<select id="findUser" resultType="bean.User"  >
			select 
				*
			from user 
			where 
				imCode=#{imCode}
		</select>
		<!-- void regiser(User user);新用户注册 -->
		<insert id="regiser" parameterType="bean.User">		
			insert into user (imCode , nickname , sex , iconurl, password) 
			values (#{imCode} , #{nickname} , #{sex} , #{iconurl} ,#{password})
		
		</insert>

	
	</mapper>
